{
  "name": "bamboozle-fault-demo",
  "version": "0.1.0",
  "private": true,
  "description": "A runnable companion to the 'Simulating faults your unit tests can't catch' blog post — uses Bamboozle to inject TCP resets, latency, empty bodies, and transient faults into a small TypeScript HTTP client.",
  "type": "module",
  "scripts": {
    "bamboozle:up": "docker compose up -d",
    "bamboozle:down": "docker compose down",
    "bamboozle:logs": "docker compose logs -f bamboozle",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "axios": "^1.7.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^6.0.0",
    "vitest": "^4.1.5"
  }
}

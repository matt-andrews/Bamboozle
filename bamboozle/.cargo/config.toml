# Dependabot cannot reach the sparse crates.io registry (index.crates.io).
# This tells Cargo to use the system git CLI for registry operations instead,
# which avoids the sparse protocol entirely without redefining any sources.
[net]
git-fetch-with-cli = true
